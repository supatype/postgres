[package]
name = "pg_keyspace_poc"
version = "0.1.0"
edition = "2021"
description = "P0 spike for pg_keyspace: shared-memory RESP keyspace hot path, benchmarked against Valkey/Redis"
license = "Apache-2.0"

[lib]
name = "pgks"
path = "src/lib.rs"

[[bin]]
name = "pgkeyspaced"
path = "src/bin/pgkeyspaced.rs"

[dependencies]
libc = "0.2"

[profile.release]
opt-level = 3
lto = "thin"
codegen-units = 1
panic = "abort"
debug = false
