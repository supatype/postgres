# P6 masked-read cost (100k rows, 3 masked columns, min of 5 runs)
  plain, no mask                                     12.207 ms
  masked, predicate = trivial (inlined)              18.960 ms
  masked, predicate = table lookup per row         1126.932 ms
  masked, predicate = supacache.get (§6)            97.237 ms
  masked, predicate = row-indep. InitPlan            18.614 ms

# Proof: the row-independent predicate plans as an InitPlan (evaluated once).
# (per-row predicates appear as a SubPlan/filter re-run for every row)
           QUERY PLAN           
--------------------------------
 Aggregate
   InitPlan 1
     ->  Result
   InitPlan 2
     ->  Result
   InitPlan 3
     ->  Result
   ->  Seq Scan on bench_masked
(8 rows)

