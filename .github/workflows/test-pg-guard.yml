name: pg_guard tests

on:
  pull_request:
    paths:
      - 'extensions/pg_guard/**'
  push:
    branches: [main]
    paths:
      - 'extensions/pg_guard/**'

jobs:
  test:
    name: pg_guard regression (PG${{ matrix.pg }})
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        pg: [17]

    steps:
      - uses: actions/checkout@v4

      # PGDG, because the runner image does not carry PostgreSQL 17: installing it from the default
      # Ubuntu repositories fails with "Unable to locate package", which is a missing repository rather
      # than a missing package.
      - name: Add the PostgreSQL apt repository
        run: |
          sudo apt-get install -y --no-install-recommends ca-certificates curl gnupg lsb-release
          sudo install -d /usr/share/postgresql-common/pgdg
          sudo curl -fsSL -o /usr/share/postgresql-common/pgdg/apt.postgresql.org.asc \
            https://www.postgresql.org/media/keys/ACCC4CF8.asc
          echo "deb [signed-by=/usr/share/postgresql-common/pgdg/apt.postgresql.org.asc] https://apt.postgresql.org/pub/repos/apt $(lsb_release -cs)-pgdg main" \
            | sudo tee /etc/apt/sources.list.d/pgdg.list

      - name: Install PostgreSQL ${{ matrix.pg }}
        run: |
          sudo apt-get update -qq
          sudo apt-get install -y --no-install-recommends \
            postgresql-${{ matrix.pg }} \
            postgresql-server-dev-${{ matrix.pg }}

      - name: Build pg_guard
        run: |
          PG_CONFIG=/usr/lib/postgresql/${{ matrix.pg }}/bin/pg_config \
            make -C extensions/pg_guard

      - name: Run regression tests
        run: |
          PG_CONFIG=/usr/lib/postgresql/${{ matrix.pg }}/bin/pg_config \
            make -C extensions/pg_guard check
        env:
          # pg_regress needs to connect to a running cluster; pg_guard's
          # test/init.sh handles cluster creation via pg_ctl.
          PGUSER: postgres

      - name: Upload regression diffs on failure
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: pg_guard-regression-diffs-pg${{ matrix.pg }}
          path: extensions/pg_guard/regression.diffs
          retention-days: 7
